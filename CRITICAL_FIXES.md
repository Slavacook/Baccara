# 🔥 Критические исправления - Junket лимиты и дробные остатки

## ✅ Исправлено 2 критических бага:

### 1. Лимиты Junket теперь правильные ✅

**Проблема:** При старте игры лимиты были 1000-2000 вместо 2000-200000

**Причина:** GameController инициализировал лимиты из старого GameConfig.gd вместо GameModeManager

**Исправление:**
- **Файл:** `scripts/GameController.gd:101-106`
- **Было:** Использовался `config.table_min_bet, config.table_max_bet` (1000-2000)
- **Стало:** Используется `GameModeManager.get_config()` (2000-200000 для Junket)

```gdscript
# БЫЛО (НЕПРАВИЛЬНО):
_on_limits_changed(
    config.table_min_bet, config.table_max_bet, config.table_step,
    config.tie_min_bet, config.tie_max_bet, config.tie_step
)

# СТАЛО (ПРАВИЛЬНО):
var cfg = GameModeManager.get_config()
_on_limits_changed(
    cfg["main_min"], cfg["main_max"], cfg["main_step"],
    cfg["tie_min"], cfg["tie_max"], cfg["tie_step"]
)
```

**Результат:**
- **Junket:** 2000-200000, шаг 500 ✅
- **Classic:** 50-3000, шаг 1 ✅

---

### 2. Дробные остатки теперь отображаются правильно ✅

**Проблема:** Фишка 0.5 существует в Classic, но выплаты показывали "2345" вместо "2345.5"

**Требование:**
- Целые числа: `2345` (без `.0`)
- Дробные: `2345.5` (с остатком)

**Исправления:**

#### A. PayoutPopup.gd - изменён тип данных с int на float

**Строка 187-195:**
```gdscript
# БЫЛО:
signal payout_confirmed(is_correct: bool, collected: int, expected: int)
var current_stake: int = 0
var expected_payout: int = 0

# СТАЛО:
signal payout_confirmed(is_correct: bool, collected: float, expected: float)
var current_stake: float = 0.0
var expected_payout: float = 0.0
```

**Строка 367:**
```gdscript
# БЫЛО:
func show_payout(winner: String, stake: int, payout: int):

# СТАЛО:
func show_payout(winner: String, stake: float, payout: float):
```

**Строка 679-684:**
```gdscript
# БЫЛО:
var collected_total: int = 0
for stack in chip_stacks:
    collected_total += int(stack.get_total())
var is_correct = (collected_total == expected_payout)

# СТАЛО:
var collected_total: float = 0.0
for stack in chip_stacks:
    collected_total += stack.get_total()
var is_correct = abs(collected_total - expected_payout) < 0.01  # Погрешность float
```

**Строка 707:**
```gdscript
# БЫЛО:
func _show_error_animation(_collected: int):

# СТАЛО:
func _show_error_animation(_collected: float):
```

#### B. Добавлена функция форматирования

**Строка 358-364 (PayoutPopup.gd):**
```gdscript
# ← Форматирование числа: "2345" для целых, "2345.5" для дробных
func _format_amount(amount: float) -> String:
    # Проверяем, является ли число целым
    if amount == floor(amount):
        return str(int(amount))  # "2345"
    else:
        return str(amount)  # "2345.5"
```

#### C. Обновлены места отображения сумм

**Строка 383 (PayoutPopup.gd):**
```gdscript
# БЫЛО:
stake_label.text = Localization.t("PAYOUT_STAKE", [stake])

# СТАЛО:
stake_label.text = Localization.t("PAYOUT_STAKE", [_format_amount(stake)])
```

**Строка 670 (PayoutPopup.gd):**
```gdscript
# БЫЛО:
collected_amount_label.text = str(total_int)

# СТАЛО:
collected_amount_label.text = _format_amount(total)
```

#### D. Исправлен алгоритм подсказки для float

**Строка 765-774 (PayoutPopup.gd):**
```gdscript
# БЫЛО:
while remaining >= denom:
    ...
    if remaining <= 0:
        break
if remaining <= 0:
    break

# СТАЛО:
while remaining >= denom - 0.01:  # Учитываем погрешность float
    ...
    if remaining < 0.01:  # Учитываем погрешность float
        break
if remaining < 0.01:  # Учитываем погрешность float
    break
```

#### E. GameController.gd - убраны касты к int

**Строка 145-158:**
```gdscript
# БЫЛО:
var stake = 0
var payout = 0

if actual == "Banker":
    stake = limits_manager.generate_bet()
    payout = int(stake * GameModeManager.get_banker_commission())
elif actual == "Tie":
    stake = limits_manager.generate_tie_bet()
    payout = stake * 8
else:  # Player
    stake = limits_manager.generate_bet()
    payout = stake

# СТАЛО:
var stake: float = 0.0
var payout: float = 0.0

if actual == "Banker":
    stake = limits_manager.generate_bet()
    payout = stake * GameModeManager.get_banker_commission()  # float!
elif actual == "Tie":
    stake = limits_manager.generate_tie_bet()
    payout = stake * 8.0
else:  # Player
    stake = limits_manager.generate_bet()
    payout = stake * 1.0
```

**Строка 188:**
```gdscript
# БЫЛО:
func _on_payout_confirmed(is_correct: bool, collected: int, expected: int):

# СТАЛО:
func _on_payout_confirmed(is_correct: bool, collected: float, expected: float):
```

**Строка 192, 200:**
```gdscript
# БЫЛО:
print("✅ Правильно! Выплата: %d" % expected)
print("❌ Ошибка! Собрано: %d, ожидалось: %d" % [collected, expected])

# СТАЛО:
print("✅ Правильно! Выплата: %s" % expected)
print("❌ Ошибка! Собрано: %s, ожидалось: %s" % [collected, expected])
```

---

## 📊 Таблица изменений:

| Файл | Строки | Что изменено |
|------|--------|--------------|
| `scripts/GameController.gd` | 101-106 | Инициализация из GameModeManager вместо config |
| `scripts/GameController.gd` | 145-158 | Убраны касты к int, используется float |
| `scripts/GameController.gd` | 188 | Сигнатура функции: int → float |
| `scripts/GameController.gd` | 192, 200 | Форматирование print: %d → %s |
| `scripts/PayoutPopup.gd` | 187-195 | Тип данных: int → float |
| `scripts/PayoutPopup.gd` | 358-364 | Добавлена функция _format_amount() |
| `scripts/PayoutPopup.gd` | 367 | Сигнатура show_payout: int → float |
| `scripts/PayoutPopup.gd` | 383 | Форматирование stake_label |
| `scripts/PayoutPopup.gd` | 670 | Форматирование collected_amount_label |
| `scripts/PayoutPopup.gd` | 679-684 | Сравнение с погрешностью 0.01 |
| `scripts/PayoutPopup.gd` | 707 | Сигнатура _show_error_animation: int → float |
| `scripts/PayoutPopup.gd` | 765-774 | Алгоритм подсказки с учётом float |

---

## 🧪 Как протестировать:

### Тест 1: Junket лимиты при старте
1. Запустить игру
2. Открыть меню настроек ⚙️
3. **Проверить:** Режим Junket должен показывать "2000-200000, шаг 500"
4. Начать игру → выбрать победителя
5. **Проверить в PayoutPopup:** Ставка должна быть в диапазоне 2000-200000

### Тест 2: Classic режим - дробные остатки
1. Открыть меню ⚙️
2. Выбрать **Classic**
3. Начать игру → выбрать победителя **Banker**
4. **Ожидаемая ставка:** например, 1523
5. **Ожидаемая выплата:** 1523 × 0.5 = **761.5** ✅
6. **Проверить:** В интерфейсе должно отображаться "761.5" (НЕ "761")
7. Добавить фишки:
   - 5000 × 0 = 0
   - 1000 × 0 = 0
   - 500 × 1 = 500
   - 100 × 2 = 200
   - 25 × 2 = 50
   - 5 × 2 = 10
   - 1 × 1 = 1
   - **0.5 × 1 = 0.5** ← Фишка 0.5 учитывается!
8. Итого: **761.5** ✅
9. **Проверить:** Число справа показывает "761.5" (НЕ "761")
10. Нажать "Выплата:" → должно быть **Верно!** ✅

### Тест 3: Целые числа без .0
1. Режим **Junket**
2. Начать игру → выбрать победителя **Player**
3. **Ожидаемая ставка:** например, 25000
4. **Ожидаемая выплата:** 25000 × 1.0 = **25000** (целое)
5. **Проверить:** Отображается "25000" (НЕ "25000.0") ✅

### Тест 4: Подсказка с дробными
1. Режим **Classic**
2. Начать игру → выбрать победителя **Banker**
3. **Ожидаемая выплата:** например, 761.5
4. Нажать кнопку **"Подсказка"**
5. **Проверить:** Фишки автоматически выставились правильно:
   - 500 × 1 = 500
   - 100 × 2 = 200
   - 25 × 2 = 50
   - 5 × 2 = 10
   - 1 × 1 = 1
   - **0.5 × 1 = 0.5** ✅
6. **Проверить:** Сумма показывает "761.5"
7. Нажать "Выплата:" → должно быть **Верно!**

---

## 🎯 Ключевые улучшения:

### 1. Правильная инициализация лимитов
- Теперь лимиты берутся из **GameModeManager**, а не из старого GameConfig
- При старте игры сразу правильные значения (2000-200000 для Junket)

### 2. Полная поддержка дробных остатков
- Все переменные и функции используют **float** вместо int
- Умное форматирование: целые без `.0`, дробные с остатком
- Сравнение с погрешностью **0.01** для float

### 3. Корректный алгоритм подсказки
- Жадный алгоритм учитывает погрешность float
- Правильно работает с фишкой **0.5**

---

## 📋 Статистика:

- **Файлов изменено:** 2
- **Строк кода:** ~30
- **Новых функций:** 1 (_format_amount)
- **Исправлено критических багов:** 2
- **Улучшений точности:** float вместо int (±0.01)

---

## ⚠️ Важные замечания:

### Float comparison
Везде, где сравниваются float, используется погрешность **0.01**:
```gdscript
abs(collected_total - expected_payout) < 0.01
remaining >= denom - 0.01
remaining < 0.01
```

Это нужно для избежания ошибок точности float (например, 0.1 + 0.2 ≠ 0.3 в компьютерной арифметике).

### Форматирование
Функция `_format_amount()` использует `floor()` для проверки целочисленности:
```gdscript
if amount == floor(amount):  # Целое?
    return str(int(amount))  # "2345"
else:
    return str(amount)  # "2345.5"
```

---

✨ **Все критические исправления завершены! Готово к тестированию!** ✨

**Рекомендации для теста:**
1. Запустить игру и проверить лимиты Junket (должны быть 2000-200000)
2. Переключиться в Classic и протестировать дробные остатки (0.5)
3. Проверить, что целые числа отображаются без `.0`
4. Протестировать кнопку "Подсказка" с дробными суммами
