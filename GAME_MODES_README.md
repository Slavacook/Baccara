# 🎮 Система режимов игры Junket/Classic + Режим выживания - Готово!

## ✅ Что реализовано

### 1. Два режима игры (Junket/Classic)

**Junket Mode:**
- Основные ставки: 2000-200,000 (шаг 500)
- Tie: 100-900 (шаг 25)
- Pairs: 100-900 (шаг 25)
- Комиссия банкира: **95%** (5% комиссия)
- Номиналы фишек: 500000, 100000, 25000, 5000, 1000, 500, 100, 50, 25, 10, 5, 1

**Classic Mode:**
- Основные ставки: 50-3000 (шаг 1)
- Tie: 25-300 (шаг 1)
- Pairs: 25-200 (шаг 1)
- Комиссия банкира: **50%**
- Номиналы фишек: 5000, 1000, 500, 100, 50, 25, 10, 5, 1, 0.5

### 2. Режим выживания (Survival Mode) ❤️

- **7 жизней** (отображаются как сердечки ❤️)
- При каждой ошибке теряется 1 жизнь
- При потере всех жизней → Game Over
- Счётчик пройденных раундов
- Возможность рестарта после Game Over
- **Включается через меню настроек** (по умолчанию выключен)

**Ошибки, за которые теряется жизнь:**
- Неправильный выбор третьей карты игрока
- Неправильный выбор третьей карты банкира
- Неправильный выбор победителя
- Неправильная выплата

### 3. Меню настроек ⚙️

- Переключение между Junket и Classic
- Переключение языка (Русский/English)
- **Включение/выключение режима выживания** (новое!)
- Автоматическое сохранение всех настроек

### 4. Автоматические обновления

При смене режима автоматически обновляются:
- Лимиты ставок
- Номиналы фишек в PayoutPopup
- Процент выплаты для банкира

## 📁 Новые файлы

1. **`scripts/GameModeManager.gd`** - Глобальный менеджер режимов (autoload)
2. **`scripts/SettingsPopup.gd`** - Скрипт меню настроек
3. **`scenes/SettingsPopup.tscn`** - Сцена меню настроек
4. **`SETTINGS_SETUP.md`** - Инструкция по настройке

## 🔧 Изменённые файлы

### scripts/SaveManager.gd
- Добавлены методы `save_settings()` и `load_settings()`
- Добавлены методы `save_survival_mode()` и `load_survival_mode()`
- Все настройки сохраняются в `user://baccarat_settings.save`

### scripts/PayoutPopup.gd
- Номиналы фишек теперь динамические (получаются из GameModeManager)
- Автоматическое обновление при смене режима
- Подписка на сигнал `mode_changed`

### scripts/GameController.gd
- Добавлены переменные `settings_popup` и `settings_button`
- Выплата банкира теперь через `GameModeManager.get_banker_commission()`
- Обработчики `_on_mode_changed()`, `_on_language_changed()`, `_on_survival_mode_changed()`
- Метод `_load_survival_mode_setting()` для загрузки настройки режима выживания
- Режим выживания теперь управляется через меню (не включён по умолчанию)

### scripts/SettingsPopup.gd (НОВОЕ)
- Добавлен сигнал `survival_mode_changed(enabled: bool)`
- Добавлена переменная `survival_mode_enabled`
- Добавлен UI элемент `survival_checkbox: CheckButton`
- Методы `_on_survival_toggled()`, `_update_survival_checkbox()`, `set_survival_mode()`

### scenes/SettingsPopup.tscn (ОБНОВЛЕНО)
- Увеличен размер окна (400x420)
- Добавлена секция "Режим выживания / Survival Mode"
- Добавлен CheckButton для включения/выключения режима

### scripts/GameModeManager.gd
- Обновлены chip_denominations (убраны 10, 50, 500000 из-за отсутствия текстур)

### project.godot
- Добавлен autoload: `GameModeManager="*res://scripts/GameModeManager.gd"`

## 🎯 Что нужно сделать вручную

### Шаг 1: Добавить SettingsPopup в Game.tscn

1. Откройте `scenes/Game.tscn` в Godot
2. Щёлкните правой кнопкой на узле `Game`
3. Выберите "Instantiate Child Scene"
4. Выберите `scenes/SettingsPopup.tscn`

### Шаг 2: Добавить кнопку настроек

1. Добавьте узел `Button` к `Game`
2. Назовите его `SettingsButton`
3. Установите:
   - Text: `⚙️`
   - Position: справа вверху (offset_left=1050, offset_top=-8)
   - Font Size: 24

### Шаг 3: Переместить сердечки вниз

1. Найдите `UI/SurvivalModeUI`
2. В Inspector → Layout → Anchors Preset выберите "Bottom Wide"
3. Или установите:
   - anchor_top = 1.0
   - anchor_bottom = 1.0
   - offset_top = -50

### Шаг 4: Скрыть старую кнопку языка

1. Найдите узел `LangButton`
2. Снимите галочку `Visible` или удалите узел

## 🔍 Как работает

### При запуске игры:
1. GameModeManager загружает сохранённый режим (по умолчанию Junket)
2. SaveManager загружает настройку режима выживания (по умолчанию выключен)
3. Применяются соответствующие лимиты и номиналы фишек
4. SettingsPopup синхронизируется с текущими настройками
5. Если режим выживания включён → показываются сердечки ❤️

### При изменении режима игры (Junket/Classic):
1. Пользователь открывает меню ⚙️ и выбирает режим
2. GameModeManager сохраняет выбор и отправляет сигнал `mode_changed`
3. PayoutPopup обновляет номиналы фишек
4. GameController обновляет лимиты ставок
5. Новая выплата банкира вычисляется по новому проценту

### При включении/выключении режима выживания:
1. Пользователь открывает меню ⚙️ и переключает CheckButton
2. SettingsPopup отправляет сигнал `survival_mode_changed`
3. GameController получает сигнал и активирует/деактивирует SurvivalModeUI
4. SaveManager сохраняет настройку в файл
5. При активации → показываются 7 сердечек ❤️
6. При деактивации → сердечки скрываются

## 🧪 Тестирование

### Проверка Junket режима:
1. Откройте меню ⚙️
2. Выберите "Junket"
3. Начните игру, выиграйте как банкир
4. В PayoutPopup должны быть крупные номиналы (100K, 50K...)
5. Ставка банкира × 0.95 = ожидаемая выплата

### Проверка Classic режима:
1. Откройте меню ⚙️
2. Выберите "Classic"
3. Начните игру, выиграйте как банкир
4. В PayoutPopup должны быть мелкие номиналы (5K, 1K, 0.5...)
5. Ставка банкира × 0.50 = ожидаемая выплата

### Проверка режима выживания:
1. Откройте меню ⚙️
2. Включите CheckButton "Включить (7 жизней)"
3. Закройте меню → должны появиться 7 сердечек ❤️
4. Сделайте ошибку (например, неправильно выберите победителя)
5. Одно сердечко должно стать чёрным 🖤
6. После 7 ошибок → должен показаться попап Game Over
7. Нажмите "Restart" → жизни восстанавливаются
8. Откройте меню ⚙️ и выключите режим → сердечки исчезают
9. Перезапустите игру → настройка сохранилась (режим остался выключен)

## 📊 Технические детали

### Архитектура:
```
GameModeManager (Autoload)
    ↓
    ├→ SaveManager (сохранение/загрузка режимов и настроек)
    ├→ GameController (лимиты, выплаты, режим выживания)
    ├→ PayoutPopup (номиналы фишек)
    ├→ SettingsPopup (UI управления)
    └→ SurvivalModeUI (отображение жизней)
```

### Сигналы:
- `GameModeManager.mode_changed(mode: String)` - изменение режима игры
- `SettingsPopup.mode_changed(mode: String)` - выбор режима в меню
- `SettingsPopup.language_changed(lang: String)` - изменение языка
- `SettingsPopup.survival_mode_changed(enabled: bool)` - вкл/выкл режима выживания
- `SurvivalModeUI.game_over(rounds_survived: int)` - конец игры (0 жизней)

## 🎨 UI Элементы

### SettingsPopup:
- Заголовок: "⚙️ НАСТРОЙКИ / SETTINGS"
- Секция режимов игры:
  - Кнопки: "Junket" / "Classic"
  - Описание лимитов
- Секция языка:
  - Кнопки: "Русский" / "English"
- **Секция режима выживания (новое!):**
  - CheckButton: "Включить (7 жизней) / Enable (7 lives)"
- Кнопка закрытия: "Закрыть / Close"

### Кнопка настроек:
- Эмодзи: ⚙️
- Расположение: правый верхний угол
- При клике открывает SettingsPopup

### SurvivalModeUI:
- Отображает 7 сердечек (❤️ или 🖤)
- Расположение: верхняя часть экрана
- Анимация мигания при потере жизни
- Скрыт по умолчанию (показывается при активации)

## 💡 Дополнительные возможности

В будущем можно добавить:
- Третий режим (например, "VIP")
- Настройка скорости анимации
- Звуковые темы для каждого режима
- История изменения режимов

---

✨ **Система готова к использованию!** ✨

После выполнения шагов 1-4 вручную всё будет работать автоматически.
