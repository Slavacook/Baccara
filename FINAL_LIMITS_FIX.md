# 🎯 Финальное исправление лимитов и логики выплат

## ✅ Исправлено 3 критических проблемы:

### 1. Лимиты теперь применяются правильно при старте ✅

**Проблема:** Тост показывал одни лимиты (50-3000), а реально использовались другие (1000-2000)

**Причина:** Функция `_on_limits_changed()` только показывала тост, но НЕ обновляла `limits_manager`

**Исправление:**
- **Файл:** `scripts/GameController.gd:103-106`
- **Было:** `_on_limits_changed(...)` - только тост
- **Стало:** `limits_manager.set_limits(...)` - обновляет лимиты И показывает тост

```gdscript
# БЫЛО (НЕ РАБОТАЛО):
_on_limits_changed(
    cfg["main_min"], cfg["main_max"], cfg["main_step"],
    cfg["tie_min"], cfg["tie_max"], cfg["tie_step"]
)  # ← Только показывает тост, не обновляет limits_manager!

# СТАЛО (РАБОТАЕТ):
limits_manager.set_limits(
    cfg["main_min"], cfg["main_max"], cfg["main_step"],
    cfg["tie_min"], cfg["tie_max"], cfg["tie_step"]
)  # ← Обновляет limits_manager, который вызывает сигнал и показывает тост автоматически
```

**Результат:**
- При старте игры лимиты теперь правильно применяются
- Тост показывает реальные лимиты

---

### 2. Игра теперь всегда начинается с Classic режима ✅

**Проблема:** Игра начиналась с Junket режима (2000-200000)

**Требование:** Всегда стартовать с Classic (50-3000)

**Исправление:**
- **Файл:** `scripts/GameModeManager.gd:9`
- **Было:** `var current_mode: Mode = Mode.JUNKET`
- **Стало:** `var current_mode: Mode = Mode.CLASSIC`

```gdscript
# БЫЛО:
var current_mode: Mode = Mode.JUNKET

# СТАЛО:
var current_mode: Mode = Mode.CLASSIC  # ← По умолчанию Classic
```

**Результат:**
- При первом запуске игры используются лимиты Classic: **50-3000, шаг 1**
- Сохранённый режим из предыдущей сессии всё ещё загружается

---

### 3. Новая логика выплат банкира в Classic режиме ✅

**Старая логика:** Банкир всегда получал 50% выплаты

**Новая логика:**
- **100% (1:1)** - обычная выплата
- **50%** - только если банкир выигрывает с **ровно 6 очками**

**Исправления:**

#### A. Изменена базовая комиссия в Classic

**Файл:** `scripts/GameModeManager.gd:37`
```gdscript
# БЫЛО:
"banker_commission": 0.50,  # 50% выплата

# СТАЛО:
"banker_commission": 1.0,  # ← 100% выплата (1:1), но 50% если банкир выигрывает с 6
```

#### B. Добавлена проверка 6 очков в GameController

**Файл:** `scripts/GameController.gd:148-158`
```gdscript
# БЫЛО:
if actual == "Banker":
    stake = limits_manager.generate_bet()
    payout = stake * GameModeManager.get_banker_commission()

# СТАЛО:
if actual == "Banker":
    stake = limits_manager.generate_bet()
    var commission = GameModeManager.get_banker_commission()

    # ← НОВАЯ ЛОГИКА для Classic: если банкир выигрывает с 6 очками → 50% выплата
    if GameModeManager.get_mode_string() == "classic":
        var banker_value = BaccaratRules.hand_value(phase_manager.banker_hand)
        if banker_value == 6:
            commission = 0.5  # ← 50% выплата при выигрыше с 6

    payout = stake * commission
```

**Результат:**
- **Classic режим:**
  - Банкир с 7, 8, 9 очками → **100% выплата** (1:1) ✅
  - Банкир с 6 очками → **50% выплата** ✅
  - Банкир с 0-5 очками → **100% выплата** (1:1) ✅
- **Junket режим:**
  - Банкир всегда → **95% выплата** (5% комиссия) ✅

---

## 📊 Итоговая таблица режимов:

| Режим | Основные ставки | Tie ставки | Выплата Banker | Выплата Player | Выплата Tie |
|-------|----------------|------------|----------------|----------------|-------------|
| **Classic** (по умолчанию) | 50-3000, шаг 1 | 25-300, шаг 1 | 1:1 (50% если 6 очков) | 1:1 | 8:1 |
| **Junket** | 2000-200000, шаг 500 | 100-900, шаг 25 | 0.95:1 (5% комиссия) | 1:1 | 8:1 |

---

## 📋 Таблица изменений:

| Файл | Строки | Что изменено |
|------|--------|--------------|
| `scripts/GameController.gd` | 103-106 | set_limits вместо _on_limits_changed |
| `scripts/GameModeManager.gd` | 9 | Режим по умолчанию: JUNKET → CLASSIC |
| `scripts/GameModeManager.gd` | 37 | banker_commission: 0.50 → 1.0 |
| `scripts/GameController.gd` | 148-158 | Добавлена проверка 6 очков банкира |

---

## 🧪 Как протестировать:

### Тест 1: Стартовые лимиты (Classic по умолчанию)
1. **Удалить сохранение:** `rm ~/Library/Application\ Support/Godot/app_userdata/баккара-5.9/save_data.json` (или через игру)
2. Запустить игру
3. **Проверить тост:** "Лимиты: 50–3000 (шаг 1)" ✅
4. Начать раунд → выбрать победителя
5. **Проверить в PayoutPopup:** Ставка в диапазоне **50-3000** ✅

### Тест 2: Переключение на Junket
1. Открыть меню ⚙️
2. Выбрать **Junket**
3. **Проверить тост:** "Лимиты: 2000–200000 (шаг 500)" ✅
4. Начать раунд → проверить ставку в диапазоне **2000-200000** ✅

### Тест 3: Выплата банкира 100% в Classic (НЕ 6 очков)
1. Режим **Classic**
2. Начать игру, дождаться пока банкир выиграет с **7, 8 или 9 очками**
3. **Пример:**
   - Ставка: 1000
   - Банкир выигрывает с **7 очками**
   - **Ожидаемая выплата:** 1000 × 1.0 = **1000** (100%) ✅
4. Выставить фишки на 1000
5. Нажать "Выплата:" → должно быть **Верно!** ✅

### Тест 4: Выплата банкира 50% в Classic (6 очков!)
1. Режим **Classic**
2. Начать игру, дождаться пока банкир выиграет с **ровно 6 очками**
3. **Пример:**
   - Ставка: 1000
   - Банкир выигрывает с **6 очками**
   - **Ожидаемая выплата:** 1000 × 0.5 = **500** (50%) ✅
4. Выставить фишки на 500
5. Нажать "Выплата:" → должно быть **Верно!** ✅

### Тест 5: Junket - всегда 95%
1. Режим **Junket**
2. Начать игру, банкир выигрывает с **любым количеством очков**
3. **Пример:**
   - Ставка: 10000
   - Банкир выигрывает с **6 очками** (или любыми)
   - **Ожидаемая выплата:** 10000 × 0.95 = **9500** (95%) ✅
4. Выставить фишки на 9500
5. Нажать "Выплата:" → должно быть **Верно!** ✅

### Тест 6: Дробные остатки в Classic (0.5)
1. Режим **Classic**
2. Банкир выигрывает с **6 очками**
3. **Пример:**
   - Ставка: 1523
   - Выплата: 1523 × 0.5 = **761.5** ✅
4. **Проверить:** Отображается "761.5" (не "761")
5. Добавить фишки включая **0.5**
6. Сумма показывает "761.5" ✅
7. Нажать "Выплата:" → должно быть **Верно!** ✅

---

## 🎯 Ключевые улучшения:

### 1. Правильная инициализация
- `limits_manager.set_limits()` вместо `_on_limits_changed()`
- Лимиты реально применяются, а не только показываются в тосте

### 2. Фиксированный стартовый режим
- Всегда начинается с **Classic** (50-3000)
- Если есть сохранение - загружается сохранённый режим

### 3. Умная логика выплат банкира
- **Classic:**
  - Обычно 100% (1:1) - справедливо для игрока
  - 50% только при выигрыше с 6 - особый случай
- **Junket:**
  - Всегда 95% (5% комиссия) - как в казино

### 4. Полная поддержка дробных остатков
- Фишка 0.5 работает корректно
- Форматирование: "2345" или "2345.5"

---

## 📋 Статистика:

- **Файлов изменено:** 2
- **Строк кода:** ~20
- **Новых функций:** 0
- **Исправлено критических багов:** 3
- **Новых логик:** 1 (проверка 6 очков)

---

## 💡 Логика выплат банкира в деталях:

### Classic режим:
```gdscript
if банкир_выигрывает:
    if очки_банкира == 6:
        выплата = ставка × 0.5  // 50%
    else:  // 0-5, 7-9 очков
        выплата = ставка × 1.0  // 100%
```

### Junket режим:
```gdscript
if банкир_выигрывает:
    выплата = ставка × 0.95  // Всегда 95% (5% комиссия)
```

---

## ⚠️ Важные замечания:

### Почему 6 очков особенный случай в Classic?
В некоторых вариантах баккара (например, EZ Baccarat) выигрыш банкира с 6 очками платит меньше (обычно 1:2 вместо 1:1), чтобы компенсировать отсутствие комиссии на остальных выигрышах.

### Проверка режима:
```gdscript
GameModeManager.get_mode_string()  // "classic" или "junket"
```

### Проверка очков руки:
```gdscript
BaccaratRules.hand_value(hand: Array[Card]) -> int  // 0-9
```

---

✨ **Все исправления завершены! Готово к тестированию!** ✨

**Рекомендации для финального теста:**
1. Удалить сохранение и проверить, что игра стартует с Classic (50-3000)
2. Протестировать выплату банкира 100% при НЕ 6 очках
3. Протестировать выплату банкира 50% при 6 очках
4. Переключиться на Junket и проверить, что выплата всегда 95%
5. Проверить дробные остатки (0.5) в обоих режимах
